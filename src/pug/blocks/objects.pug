section.objects
  .heading(
    data-language="objects-title"
  )!="<?= $language['objects']['title'] ?>"
  !="<? foreach($language['objects']['items'] as $index => $data): ?>"
  .object
    .description
      .title(
        data-language!="objects-items-<?= $index ?>-title"
      )!="<?= $language['objects']['items'][$index]['title'] ?>"
      .text(
      data-language!="objects-items-<?= $index ?>-text"
      )!="<?= $language['objects']['items'][$index]['text'] ?>"
    .image(class!="temple_<?= $index ?>")
  !="<? endForeach; ?>"